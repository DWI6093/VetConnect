services:
  frontend-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev-frontend
    container_name: angular-dev
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "4200:4200"
    environment:
      - NODE_ENV=development
    stdin_open: true
    tty: true
